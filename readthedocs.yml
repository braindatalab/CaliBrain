# filepath: /Users/orabe/0.braindata/CaliBrain/readthedocs.yml
version: 2

build:
  os: ubuntu-22.04 # Use a recent LTS version
  tools:
    python: "3.10"

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - requirements: docs/requirements.txt # Point to doc-specific requirements
    - method: pip
      path: . -e
      editable: true